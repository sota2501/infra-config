---
# TODO: terraform apply 後、実際の IP に置き換える(terraform.tfvars.example のサンプル値と対応させてある)。
# 台数を変えた場合はここも合わせて更新する。

all:
  children:
    k8s_cluster:
      children:
        control_plane:
          hosts:
            iris-k8s-cp-1:
              ansible_host: 192.168.11.192
        workers:
          hosts:
            iris-k8s-wk-1:
              ansible_host: 192.168.11.208
      vars:
        ansible_user: cloudinit
        ansible_python_interpreter: /usr/bin/python3
