---
- name: private レジストリ(ghcr.io)用の imagePullSecrets を登録する
  hosts: control_plane
  become: true
  tasks:
    - name: image_pull_secrets ロールを実行
      ansible.builtin.include_role:
        name: image_pull_secrets
      when: inventory_hostname == kubeadm_init_host
